<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Error Pages</title>
    <style>
        /* Base Styles */
        body {
            font-family: 'Segoe UI', sans-serif;
            background: #f8f9fa;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
        }

        .error-container {
            background: white;
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            width: 400px;
            text-align: center;
        }

        .error-icon {
            width: 64px;
            height: 64px;
            margin-bottom: 1.5rem;
        }

        .error-title {
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }

        .error-message {
            color: #6b7280;
            margin-bottom: 2rem;
            line-height: 1.5;
        }

        .action-buttons {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .btn {
            padding: 0.75rem 1.5rem;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.2s;
        }

        .btn-primary {
            background: #3b82f6;
            color: white;
            border: 2px solid #3b82f6;
        }

        .btn-secondary {
            background: white;
            color: #3b82f6;
            border: 2px solid #3b82f6;
        }

        /* Specific Error Styles */
        .user-not-found { color: #1d4ed8; }
        .wrong-password { color: #dc2626; }
    </style>
</head>
<body>
    <!-- User Does Not Exist Page -->
    <div class="error-container" style="display: none;"> <!-- Remove style to show -->
        <svg class="error-icon user-not-found" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                  d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
        </svg>
        <h2 class="error-title user-not-found">Account Not Found</h2>
        <p class="error-message">
            The provided username does not exist in our system.<br>
            Please check your credentials or contact your administrator.
        </p>
        <div class="action-buttons">
            <a href="#" class="btn btn-primary">Try Again</a>
            <a href="#" class="btn btn-secondary">Contact Support</a>
        </div>
    </div>

    <!-- Incorrect Password Page -->
    <div class="error-container"> <!-- Remove this container to show user-not-found -->
        <svg class="error-icon wrong-password" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                  d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
        </svg>
        <h2 class="error-title wrong-password"><%= message %></h2>
        <p class="error-message">
            <%= text_message %>
        </p>
        <div class="action-buttons">
            <a href="/login" class="btn btn-primary">Login</a>
            <a href="/register" class="btn btn-secondary">Register</a>
        </div>
    </div>
</body>
</html>